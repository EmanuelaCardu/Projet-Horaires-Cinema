@model IEnumerable<ASP_Projet_Cinema.Models.DiffusionListItemViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiffusionDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiffusionTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AudioLang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubTitleLang)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiffusionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiffusionTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AudioLang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubTitleLang)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_Diffusion }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id_Diffusion }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id_Diffusion })
            </td>
        </tr>
}
    </tbody>
</table>
